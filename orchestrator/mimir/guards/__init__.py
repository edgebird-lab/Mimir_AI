"""Guards: deterministic checks applied around the model and around every primitive call.

None of these rely on model cooperation. They are the topological controls that hold even when
the model is fully jailbroken (see the plan's "push every guarantee into topology" principle).
"""
