# Extra Python dependencies for the NATIVE Windows build, on top of orchestrator/requirements.txt.
# These power the docproc service (document extraction for RAG + import). All are pure-Python wheels
# (no native build), so they install cleanly into the bundled embeddable Python.
#
# NOT included (Linux-only / heavy native stacks): weasyprint + matplotlib + pandocfilters. Those drive
# the PDF-export and math-SVG paths, which need the GTK runtime; on Windows PDF export is unavailable
# and docx/html/odt/epub export still works via bundled pandoc.exe.
pdfminer.six>=20231228
python-docx>=1.1.0
python-pptx>=0.6.23
